import SwiftUI

extension Color {
    /// #F84EA6 — the app's signature pink used on event cards and accents
    static let appPink = Color(red: 248 / 255, green: 78 / 255, blue: 166 / 255)
    /// Paywall “recommended” / yearly trial card border (gold).
    static let paywallGold = Color(red: 0.86, green: 0.68, blue: 0.20)
}
